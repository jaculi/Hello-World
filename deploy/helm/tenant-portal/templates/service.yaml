apiVersion: v1
kind: Service
metadata:
  name: {{ include "tenant-portal.name" . }}
  labels:
    {{- include "tenant-portal.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector:
    {{- include "tenant-portal.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
