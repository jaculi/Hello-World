apiVersion: v1
kind: Service
metadata:
  name: {{ include "tenant-bc.name" . }}
  labels:
    {{- include "tenant-bc.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector:
    {{- include "tenant-bc.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.service.httpPort }}
      targetPort: http
    - name: grpc
      port: {{ .Values.service.grpcPort }}
      targetPort: grpc
