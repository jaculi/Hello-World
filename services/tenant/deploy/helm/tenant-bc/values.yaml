replicaCount: 2

image:
  repository: jsl/tenant-bc
  tag: ""
  pullPolicy: IfNotPresent

service:
  type: ClusterIP    # 仅集群内可达，对外统一经 APISIX 网关（BP-02 §2.1）
  httpPort: 8000
  grpcPort: 9000

resources:
  requests:
    cpu: 100m
    memory: 128Mi
  limits:
    cpu: "1"
    memory: 512Mi
